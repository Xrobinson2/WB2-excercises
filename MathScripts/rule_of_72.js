"use strict"

//sample inputs

var interestRate = .8;
var years = 10;
var savingsAccount = 1023;
var theRule = 72;
var totalDue;
var amount;

//calculation
 
var amount= theRule / interestRate 
var totalDue = amount * 2

// output

console.log(" At a " + interestRate + " interest rate your savings account will be worth " + totalDue + " in " + years + " years")